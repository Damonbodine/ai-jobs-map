# Handoff: Blueprint Light SP2 → SP3 → SP4 (execute all three today)

**For the incoming Claude session in this repo (/Users/damonbodine/ai-jobs-map).**
Written 2026-07-09 by the session that shipped SP1. Damon's instruction: complete
SP2, SP3, and SP4 today, in order, with a Damon screenshot-gate + production
deploy at the end of each sub-project.

## What this project is

Timeback (this repo, timeback.clearroadlabs.com) is being unified with
clearroadlabs.com under one design system: **Blueprint Light** — CRL is
ice-on-ink (dark); Timeback is ink-on-ice (light). Same families, inverted.

**Read these two documents before anything else:**
1. The approved spec (SP2/SP3/SP4 sections are your requirements):
   `docs/superpowers/specs/2026-07-09-blueprint-light-unification-design.md`
2. The brand contract (canonical tokens, CTA grammar, voice):
   `docs/brand-contract.md`

Also skim the progress ledger for what's already done and the ops lessons:
`.superpowers/sdd/progress.md`

## State as of this handoff

- **SP1 is SHIPPED to production** (main `bf90487`, pushed): native Tailwind v4
  `@theme` Blueprint Light tokens in `app/globals.css`; `lib/brand.ts` (BRAND +
  DATA_SERIES) with a CSS↔TS lockstep test; Geist/Geist Mono fonts; dark-mode
  machinery deleted; `docs/brand-contract.md` in both repos.
- Damon approved SP1's look ("yeah i think its right") — the light background
  is BY DESIGN; do not darken the product.
- Unit suite: 52 tests, `npm test`. E2E baseline: **12 passed / 3 pre-existing
  failures** (2 in `tests/e2e/build-a-team.spec.ts`, 1 in
  `tests/e2e/occupation-inline-builder.spec.ts`) — acceptance everywhere is
  "no NEW failures," not "all green."

## Process (Damon's chosen model)

- **The session model plans and reviews; SONNET subagents write the code**
  (superpowers:writing-plans per SP → superpowers:subagent-driven-development,
  fresh Sonnet implementer per task, Sonnet task reviewers, final whole-branch
  review per SP on the session model).
- Per SP: feature branch → plan with complete code per task → execute →
  screenshots (1440 + 390) to the scratchpad → **STOP for Damon's explicit
  go** → merge to main → push (production deploy) → ledger update → next SP.
- The brainstorm/spec phase is DONE — do not re-open decisions. Requirements
  come from the spec; if you find a genuine spec gap, ask Damon, don't invent.
- Keep a per-task ledger in `.superpowers/sdd/progress.md` (append-only).

## Hard-won ops knowledge (violating these has already caused real failures)

1. **NEVER `git add -A` or `git add .`** — untracked QA screenshots and
   `.env.local`/`.env.production.local` sit at repo root. Explicit paths only.
2. **NEVER run `tests/e2e/live-qa.spec.ts`** — it submits REAL forms to
   production. Local e2e = exactly: `tests/e2e/build-a-team.spec.ts
   tests/e2e/contact-form.spec.ts tests/e2e/funnel-realness.spec.ts
   tests/e2e/occupation-inline-builder.spec.ts`. (SP4 fences live-qa properly.)
3. **npm installs need `--force`, NOT `--legacy-peer-deps`** — the latter
   silently drops `@testing-library/dom` and breaks all unit tests (verified).
4. `npm run lint` does not work — ESLint was never configured in this repo
   (known follow-up; do NOT bootstrap it mid-task). Gates are: `npm run
   type-check`, `npm test`, `npm run build`, the four local e2e specs.
5. Dev server port 3050; Playwright starts its own server on 3070.
6. Screenshot harness: node script importing
   `/Users/damonbodine/ai-jobs-map/node_modules/@playwright/test/index.js`
   (CJS default-import: `import pkg from '...'; const { chromium } = pkg;`).
7. Do not touch: `lib/site.ts` (AGENCY constants — all 31 clearroadlabs.com
   links), `lib/analytics.ts` GA4 taxonomy, `app/api/**` behavior (SP3 only
   restyles email HTML in routes, never logic), DB/Neon, Next.js version.
8. The lockstep test (`tests/unit/brand-lockstep.test.ts`) must stay green —
   if you add tokens to `globals.css` that PDF/email/OG need, add them to
   `lib/brand.ts` AND the test.
9. `dark:` variant is class-gated via `@custom-variant dark` in globals.css —
   nothing applies `.dark`, so `dark:` classes are inert. SP2 should DELETE
   leftover `dark:` classes it encounters while restyling (36 exist), not
   preserve them.

## SP2 — Apply Blueprint Light (the big one; branch `feature/sp2-apply-blueprint-light`)

Spec section "SP2". Summary of scope:
- Header + Footer to CRL grammar: mono uppercase nav (Geist Mono, tracking
  ~0.16em), cyan (`bg-cyan` #6fd4ec) + ink-text CTA button replacing the
  current ink-filled one, hairline borders, ice-tint hovers.
- Pages in review order: `/` → `/occupation/[slug]` → `/build-a-team` →
  `/browse` + `/category/[category]` → `/demo` + `/demo/[slug]` + `/demo/try`
  → `/products` `/about` `/principles` `/contact` → legal pages.
- Charts to `DATA_SERIES` from `lib/brand.ts`: `components/TimeDonut.tsx`
  (11 hexes), `app/occupation/[slug]/occupation-builder.tsx` (11),
  `app/build-a-team/role-builder.tsx` (11); `components/demo/AgentLoopDiagram.tsx`
  (5) + `DemoTimeline.tsx` (2) to BRAND constants.
- Terminal/demo panels: literal CRL-dark islands (bg terminal #0a1420, text
  #e3ecf4, cyan accents) — replace residual indigo/purple accents.
- Mono eyebrows + hairline motifs where CRL uses them (section labels, stat
  lines, tabular-nums on data).
- Delete `dark:` classes encountered in touched files (see ops note 9).
- `lib/modules.ts` module-accent color map → DATA_SERIES-derived.
- Light copy polish ONLY where a touched money page reads off-voice.
- Screenshots per money page before/after; STOP for Damon's gate.

Plan-writing note: read the actual component files before writing the plan —
SP1's briefs quoted exact current code and that made tasks near-mechanical.
Big files: occupation-builder.tsx (751 lines), blueprint-view.tsx (557).

## SP3 — Non-CSS surfaces (branch `feature/sp3-brand-surfaces`)

Spec section "SP3". Summary:
- `lib/pdf/styles.ts` (17 hexes) + `lib/pdf/team-deck.tsx` (9) import from
  `lib/brand.ts` (small adapter fine). Also clean the stale Newsreader/Manrope
  comments in `lib/pdf/blueprint.tsx`/`department.tsx` (comment-only, flagged
  in SP1).
- Email templates: 22 inline hexes across `app/api/contact`, `app/api/inquiries`,
  `app/api/one-pager`, `app/api/build-a-team/inquiry`, `app/api/build-a-team/pdf`,
  `app/api/demo/lead` → BRAND constants (extract a tiny shared email-shell
  helper if the routes repeat layout HTML). Restyle only — zero logic changes.
- `app/opengraph-image.tsx` (7 hexes) re-mirrored from BRAND with a comment
  pointing at the lockstep source.
- Gate evidence: render one blueprint PDF + one team deck to the scratchpad
  (there are API routes/lib functions for this — a small node script can call
  the PDF lib directly); write each email template's HTML to scratchpad files.
  These surfaces have ZERO test coverage — the renders ARE the verification.

## SP4 — UX grammar + hygiene (branch `feature/sp4-grammar-hygiene`)

Spec section "SP4". Summary:
- Reverse bridge: occupation page links CRL's sample audit ("See what the full
  audit looks like →" → https://clearroadlabs.com/sample-audit) — completing
  the loop (CRL's /sample-audit already links Timeback's occupation map).
- CTA language parity per the brand contract.
- Fence `tests/e2e/live-qa.spec.ts`: skip unless `RUN_LIVE_QA=1`
  (`test.skip(process.env.RUN_LIVE_QA !== "1", ...)` at the top).
- Delete stale `drizzle/schema.ts` (TypeScript file only — `lib/db/schema.ts`
  is active; NO db migration). Verify nothing imports it first.
- Remove the ~24 stray QA screenshots at repo root (both tracked like
  `01-homepage.png` and untracked `neon-occ-*.png` etc. — `git rm` the tracked,
  `rm` the untracked; also `home-snapshot.md`).
- Archive the neon branch: `git tag archive/neon-orbital-builder
  feat/neon-orbital-builder && git branch -D feat/neon-orbital-builder`
  (push the tag; delete `origin/pm-gap-sweep` remote branch too if Damon
  confirms — ask at the gate).

## Definition of done (today)

Three production deploys, each gated by Damon: Timeback fully Blueprint Light
on every surface (web, PDF, email, OG), bidirectional funnel links live, the
live-qa landmine fenced, the neon branch archived, ledger + memory updated.
End by suggesting Damon record any session learnings to his CRL-project memory
(this repo's session can't write that memory — tell him what's worth saving).
